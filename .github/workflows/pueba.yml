name: Hola Mundo Workflow

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Paso Hola Mundo
      run: echo "Hola desde GitHub Actions"
    - name: Debug
      run:  |
        echo "'Debugging: GITHUB_REF: ${{github.ref}}"
        echo "'Debugging: GITHUB_OS: ${{runner.os}}'"